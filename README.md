# Angular 2 Validation using Reactive forms

## Instructions


 Run the following to install the project dependencies:

    `npm install`
    
 Run the npm start command to build the code, watch for file changes, and serve up the site locally:

    `npm start`


